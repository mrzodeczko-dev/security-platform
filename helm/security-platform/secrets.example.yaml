# Copy this file to secrets.yaml and fill in real values.
# Usage: helm install security-platform ./helm/security-platform -f secrets.yaml

secrets:
  mysql:
    rootPassword: "change-me"
    user: "app_user"
    password: "change-me"
  redis:
    password: "change-me"
  jwt:
    secret: "change-me-base64-encoded-512bit-key"
  internalSecret: "change-me"
  mail:
    password: ""
