Security Platform has been deployed.

{{- if .Values.ingress.enabled }}

Access the API gateway at:
{{- range .Values.ingress.hosts }}
  https://{{ .host }}
{{- end }}
{{- else }}

Get the gateway URL by running:
  export NODE_PORT=$(kubectl get svc {{ .Release.Name }}-api-gateway \
    -n {{ .Release.Namespace }} -o jsonpath='{.spec.ports[0].nodePort}')
  export NODE_IP=$(kubectl get nodes -o jsonpath='{.items[0].status.addresses[0].address}')
  echo "Gateway: http://$NODE_IP:$NODE_PORT"
{{- end }}

Verify all pods are running:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/part-of=security-platform
